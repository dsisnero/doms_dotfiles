#!/usr/bin/env zsh
# vim: set ft=zsh:
if [[ $- != *i* ]]; then
  return
fi

#########################################
# ssh-agent
keychain -q --nogui ~/.ssh/id_github

#source ~/.keychain/${HOST}-sh
#########################################

if [[ -f <%= node[:repos] %>/github.com/dylanaraps/neofetch/neofetch ]]; then
  <%= node[:repos] %>/github.com/dylanaraps/neofetch/neofetch
fi
