[include]
  path = <%= File.join(@config_dir, "config.common") %>
  path = <%= File.join(@config_dir, "platforms", @platform) %>
<% if @is_wsl %>
  path = <%= File.join(@config_dir, "platforms/wsl") %>
<% end %>
